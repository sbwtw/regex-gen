
use transtable::TransTable;

pub struct ExecuteEngine {
    transtable: TransTable,
}

#[cfg(test)]
mod test {
    use transtable::*;
    use execute_engine::*;

    #[test]
    fn test_execute_engine() {
    }
}

